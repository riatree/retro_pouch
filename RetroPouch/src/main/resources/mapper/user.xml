<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userM">
	<insert id="userJoinGo" parameterType="com.scriptwhale.pouch.vo.UserVO">
		INSERT INTO users(id, password, nickname, name, phone, birthDate, sex, status, last_login, regdate)
		VALUES(#{id},#{password},#{nickname},#{name},#{phone},sysdate(),#{sex},'A',sysdate(),sysdate())
	</insert>
	
	<select id="userlogin" parameterType="com.scriptwhale.pouch.vo.UserVO" resultType="com.scriptwhale.pouch.vo.UserVO">
		SELECT *
		FROM users
		WHERE id = #{id}
		AND password = #{password}
	</select>
</mapper>